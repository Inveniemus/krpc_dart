name: krpc_dart
description: A library for Kerbal Space Program kRPC server
version: 0.1.0

repository:

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  web_socket_channel: ^1.1.0
  protobuf: ^1.1.0
  mustache_template: ^1.0.0+1

dev_dependencies:
  lint: ^1.5.1
  test: ^1.15.7
